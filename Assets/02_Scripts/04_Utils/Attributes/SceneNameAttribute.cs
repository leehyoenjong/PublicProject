using UnityEngine;

namespace PublicFramework
{
    /// <summary>
    /// 문자열 필드를 Build Settings의 씬 이름 드롭다운으로 표시.
    /// </summary>
    public class SceneNameAttribute : PropertyAttribute
    {
    }
}
