using UnityEngine;

namespace PublicFramework
{
    /// <summary>
    /// Inspector에서 편집 불가(표시만)로 만드는 Attribute.
    /// </summary>
    public class ReadOnlyAttribute : PropertyAttribute
    {
    }
}
